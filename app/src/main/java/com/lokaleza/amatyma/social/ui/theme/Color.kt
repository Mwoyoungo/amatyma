package com.lokaleza.amatyma.social.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Colour tokens pulled directly from the index(1) social design so the
 * Compose UI matches the mockup "as-is". Black-first, crimson accent.
 */
val AmaBlack          = Color(0xFF000000)
val AmaCrimson        = Color(0xFFD6324D) // primary accent / active
val AmaRose           = Color(0xFFD6577A) // secondary rose (icons on tinted chips)
val AmaPurple         = Color(0xFFC693F0) // topic / hashtag accent
val AmaDeepPurple     = Color(0xFF7D3BF0) // reaction accent

val AmaTextPrimary    = Color(0xFFF6F7FB)
val AmaTextSecondary  = Color(0xFF8892A3)
val AmaTextMuted      = Color(0xFF6E7787) // inactive nav

val AmaHairline       = Color(0x12FFFFFF) // ~rgba(255,255,255,0.07) dividers/borders
val AmaSurface        = Color(0x0AFFFFFF) // ~rgba(255,255,255,0.04) raised surface
val AmaCrimsonTint    = Color(0x24D6324D) // rgba(214,57,77,0.14) icon-button bg
val AmaCrimsonBorder  = Color(0x33D6324D) // rgba(214,57,77,0.2) icon-button border
